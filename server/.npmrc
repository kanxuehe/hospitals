# Use the public npm registry for this project.
# The user's global ~/.npmrc points at a corporate Nexus proxy that is
# missing some tarballs (e.g. iconv-lite@0.7.3), which breaks installs.
# Override per-project so installs are reliable. Remove this file to
# fall back to the global registry.
registry=https://registry.npmjs.org/
